meta {
  name: InsertItem
  type: http
  seq: 3
}

post {
  url: {{protocol}}://{{url}}:{{port}}/items
  body: json
  auth: inherit
}

body:json {
  {
    "code": "124",
    "description": "Divano"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
